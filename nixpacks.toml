[phases.install]
cmds = ["NODE_ENV=development pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @aev/backend run db:generate", "pnpm --filter @aev/backend build"]

[start]
cmd = "pnpm --filter @aev/backend run db:migrate:deploy && pnpm --filter @aev/backend start"
